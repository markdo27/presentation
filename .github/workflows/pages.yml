name: Deploy deck to Pages

on:
  push:
    branches: [main]
    paths:
      - vibecoding-deck.html
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      # The deck stays editable at vibecoding-deck.html; Pages just needs it
      # to be the directory index. Copying keeps one source of truth.
      - name: Assemble site
        run: |
          mkdir -p _site
          cp vibecoding-deck.html _site/index.html
          cp vibecoding-deck.html _site/404.html

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4

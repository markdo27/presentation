# Local defaults. No passwords are stored in this file.
HOST=127.0.0.1
PORT=8778
DATA_DIR=.data
# For hosting, set NODE_ENV=production and APP_ORIGIN to the exact HTTPS URL.
# Use one server instance and a persistent DATA_DIR volume.
# NODE_ENV=production
# HOST=0.0.0.0
# APP_ORIGIN=https://presentation.example.com
